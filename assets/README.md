# Assets Folder

Optional files you can add here:

- `logo-square.pdf` for top-right frame title logo
- `logo-circle.pdf` for circular progress mode logo
- `logo-title.pdf` for title page corner logo
- `title-bg.pdf` for title page full background

You can use `.pdf`, `.png`, or `.jpg` by changing file names in `gotham-personal.tex`.
